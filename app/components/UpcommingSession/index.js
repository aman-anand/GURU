/**
 *
 * UpcommingSession
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
// import styled from 'styled-components';

function UpcommingSession() {
  return <div />;
}

UpcommingSession.propTypes = {};

export default memo(UpcommingSession);
