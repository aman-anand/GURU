const __wpo = {
  "assets": {
    "main": [
      "/b7c9e1e479de3b53f1e4e30ebac2403a.woff",
      "/ced611daf7709cc778da928fec876475.eot",
      "/d41f55a78e6f49a5512878df1737e58a.ttf",
      "/354b3e8b3d2379621200e26eb4b1511d.svg",
      "/favicon.ico",
      "/92c9db83163207f1b8239ea93c890951.png",
      "/a00bd3cf2312636cffd262cfd50f3b9a.png",
      "/cb01bfd5df2c0bb8b3ee06a8e2bb256c.jpg",
      "/c6bbb1c9229f10e2616370ff295f2f82.png",
      "/runtime.c9f79dad78b9591c7a47.js",
      "/"
    ],
    "additional": [
      "/npm.material-ui.030c26b72836fd1bf24d.chunk.js",
      "/1.623e6a5ab000ab9e7f3b.chunk.js",
      "/npm.dom-helpers.f712ff5a1180a732862b.chunk.js",
      "/npm.react-transition-group.2997cd251faff4e690c5.chunk.js",
      "/4.f02cf313fc93fccfa00f.chunk.js",
      "/5.61b5e5251a1afdeab945.chunk.js",
      "/6.4657bbefd11974c564fd.chunk.js",
      "/7.835a29852652466fab09.chunk.js",
      "/npm.css-loader.cb69cd9c97b48d0d3f8e.chunk.js",
      "/npm.intl.9e59fd74d50402191fad.chunk.js",
      "/10.c51fe241d1a8cbaecad8.chunk.js",
      "/11.502bdac94f07bb8157c5.chunk.js",
      "/main.5313e2fcb35fa29d2497.chunk.js",
      "/npm.axios.75974b270943800f1e4e.chunk.js",
      "/npm.babel.296823a89176cbd5e4c1.chunk.js",
      "/npm.connected-react-router.1afe90e2b2dd242d27b8.chunk.js",
      "/npm.css-vendor.0952aac2dac4f9fae078.chunk.js",
      "/npm.enquire.js.9a7af52a1aa3436e84b8.chunk.js",
      "/npm.lodash.6855c7a4e0b7e768af89.chunk.js",
      "/npm.react-app-polyfill.2f2e070d003b614a0fa2.chunk.js",
      "/npm.react-redux.40a56e16553988c9bdc1.chunk.js",
      "/npm.react-slick.a3434e1fe48395c3980a.chunk.js",
      "/npm.react-swipeable-views.f7df00d6c0be06ddeb5b.chunk.js",
      "/npm.slick-carousel.b1243d168f4cb07dc09f.chunk.js",
      "/25.fbb239ebba74bdd08058.chunk.js",
      "/26.51f50bdd7a3b61918bb3.chunk.js",
      "/27.3ee1961136d1ef6a4cdc.chunk.js",
      "/28.35b2140c88cd291dfefb.chunk.js",
      "/29.5b361e8408a071474b95.chunk.js",
      "/30.ffcf91711285983dc628.chunk.js",
      "/31.df403439b3075f1dc1ee.chunk.js",
      "/32.014126cdf0af1b8e10fa.chunk.js",
      "/33.42f4d6683473332dba8d.chunk.js",
      "/34.1889d1e1deba1b4f2e35.chunk.js",
      "/35.a7df6293201b3acdd371.chunk.js",
      "/36.2c92ea1712d6fd3d7758.chunk.js",
      "/37.8d3c8686a1fa43f71e7e.chunk.js",
      "/38.46da43978d7cabc2b1e8.chunk.js",
      "/39.7e6d84c2104fc86bf3d1.chunk.js",
      "/40.a0a8e0409b6734c6ba5d.chunk.js",
      "/41.74ba03597aabcd784f20.chunk.js",
      "/42.f9d8214c338145751798.chunk.js",
      "/43.2f7a184d48b626beac95.chunk.js",
      "/44.b218f1d6933b32290f6f.chunk.js",
      "/45.c7beb01a0a0bbbd0e58d.chunk.js",
      "/46.66b25c6e7132659fc1d9.chunk.js",
      "/47.b3e3a3b78fb30061f124.chunk.js",
      "/48.45c8aa1b82af39f320ee.chunk.js",
      "/49.55fd4cf74eeb4336ae01.chunk.js",
      "/50.823f2507d3050ff4397f.chunk.js",
      "/51.712da5da2c7c7a0b3506.chunk.js",
      "/52.31cf34b750a2182d1831.chunk.js",
      "/53.e5bcea0852f63497e20c.chunk.js"
    ],
    "optional": []
  },
  "externals": [],
  "hashesMap": {
    "af91c12f0f406a4f801aeb3b398768fe41d8f864": "/b7c9e1e479de3b53f1e4e30ebac2403a.woff",
    "2dff0768f4c0a53228761eab917e2c65556042d4": "/ced611daf7709cc778da928fec876475.eot",
    "3331eebdd4ba348ef25abe00c39ffbe867d46575": "/d41f55a78e6f49a5512878df1737e58a.ttf",
    "efc9492a290a2fb3720b3c5bb5a469775e693bbb": "/354b3e8b3d2379621200e26eb4b1511d.svg",
    "5e53ef208f3fa79a48f8374488f473c76e1233de": "/favicon.ico",
    "4a39d6bc4a2782ddd335cd07ed5e79030b10257c": "/92c9db83163207f1b8239ea93c890951.png",
    "3a61aa839f2ab66c4071ea52ea655bcc17ad53e0": "/a00bd3cf2312636cffd262cfd50f3b9a.png",
    "24ed9a27bda4d34918896cc632cdfec0f35bb44a": "/cb01bfd5df2c0bb8b3ee06a8e2bb256c.jpg",
    "5046dad658e24c72fa333e468df33b4af97bf875": "/c6bbb1c9229f10e2616370ff295f2f82.png",
    "e607857fef3cee608b4c99b026db0e7b013a33e5": "/npm.material-ui.030c26b72836fd1bf24d.chunk.js",
    "dcca75410862574b3837c8a48beea619b916a5a8": "/1.623e6a5ab000ab9e7f3b.chunk.js",
    "13dc9ea4f07c40fc5d5be8e87fda06b1ab284863": "/npm.dom-helpers.f712ff5a1180a732862b.chunk.js",
    "5d20f27a838198e70a29dd0dc96d11b5f8a3360b": "/npm.react-transition-group.2997cd251faff4e690c5.chunk.js",
    "342a08931597328be12caa3ae15e585041e59696": "/4.f02cf313fc93fccfa00f.chunk.js",
    "b3d4ebb8760e59116960977ce1f7364b7ee8a601": "/5.61b5e5251a1afdeab945.chunk.js",
    "fde5ac2645b8c9995ed9ec65fe053f18d01e4381": "/6.4657bbefd11974c564fd.chunk.js",
    "855cd4ce48b4d31014323c092b422547d9b2508c": "/7.835a29852652466fab09.chunk.js",
    "0b5291f02ce9fe4c2c721468f3ce809703d5e794": "/npm.css-loader.cb69cd9c97b48d0d3f8e.chunk.js",
    "ccd406f357b04045858520bd50cc97a2d6098c5f": "/npm.intl.9e59fd74d50402191fad.chunk.js",
    "2d41a63785668fcddde5f7ce5978a9d2553d79a6": "/10.c51fe241d1a8cbaecad8.chunk.js",
    "ef4523dacea9a2e19f20d752dd0006b6653ed93c": "/11.502bdac94f07bb8157c5.chunk.js",
    "9dc80559cdb0f7441d6209a1c17b201ac8426d73": "/main.5313e2fcb35fa29d2497.chunk.js",
    "a7aace07698421e684389263ffe18dcfe759e37e": "/npm.axios.75974b270943800f1e4e.chunk.js",
    "2f5a2e6a987472abcffce374c7bf544e5e42cdea": "/npm.babel.296823a89176cbd5e4c1.chunk.js",
    "46662a3aafd08916db56b10de412a30d5bbe9af7": "/npm.connected-react-router.1afe90e2b2dd242d27b8.chunk.js",
    "6258e3b512e78a39e59a199e5d906a86aa2a88ae": "/npm.css-vendor.0952aac2dac4f9fae078.chunk.js",
    "786ecaf82113b78b8d0a16ab799864daa3804b7b": "/npm.enquire.js.9a7af52a1aa3436e84b8.chunk.js",
    "2d44d19e80a32f37caf49b2bf4cb615b890804d7": "/npm.lodash.6855c7a4e0b7e768af89.chunk.js",
    "e66f10ace06cb9b5e1d8842622421e3291ec9daa": "/npm.react-app-polyfill.2f2e070d003b614a0fa2.chunk.js",
    "d1beb5e49f0998d715ad272f8f6ebf1609da62ee": "/npm.react-redux.40a56e16553988c9bdc1.chunk.js",
    "3d698b0026b89f9ca8e3cea474c4ecaed0f96bad": "/npm.react-slick.a3434e1fe48395c3980a.chunk.js",
    "a795f94aca8baec676cb49c794798c4d6e6ede5d": "/npm.react-swipeable-views.f7df00d6c0be06ddeb5b.chunk.js",
    "642ac0f71d30a11014b2210c9ea8fec50ae9693b": "/npm.slick-carousel.b1243d168f4cb07dc09f.chunk.js",
    "c0884c6798b9725c705fe7da426900feeb17b4de": "/runtime.c9f79dad78b9591c7a47.js",
    "e76bc84848053fc8c75c96c84d7eafb8902c80b3": "/25.fbb239ebba74bdd08058.chunk.js",
    "29cc08e16422f42dca899b992879d442daa97235": "/26.51f50bdd7a3b61918bb3.chunk.js",
    "830adf6b7370130f7f6e43b57d0a091a8af3a3fb": "/27.3ee1961136d1ef6a4cdc.chunk.js",
    "b4dc0e036f6a6f91886d9c12057046fa06e837e3": "/28.35b2140c88cd291dfefb.chunk.js",
    "7e077541a5e0b5bfce8706b5575dbdee18d50cf9": "/29.5b361e8408a071474b95.chunk.js",
    "a1eca84a16e1d56b47f2a25b9148c378483faeee": "/30.ffcf91711285983dc628.chunk.js",
    "838795044bda2d7b7ac22ec799ae4e6e89b36dc0": "/31.df403439b3075f1dc1ee.chunk.js",
    "5d093bdf457100de1dd597448eaf4161b5523c63": "/32.014126cdf0af1b8e10fa.chunk.js",
    "79982976635918943abdc87c5ec2ee6325e48aba": "/33.42f4d6683473332dba8d.chunk.js",
    "60c98692306f60c45721d672ff8a85d8ec7176d9": "/34.1889d1e1deba1b4f2e35.chunk.js",
    "a9ed719808c8f50648caacb1815a64f90991d142": "/35.a7df6293201b3acdd371.chunk.js",
    "b66eab377e08fd06094c561dee3d547eb1746395": "/36.2c92ea1712d6fd3d7758.chunk.js",
    "88d7890a0f961364c2a2e8270feb8c8c83927077": "/37.8d3c8686a1fa43f71e7e.chunk.js",
    "ab5eadf23c1e6ead0904cf90ad727a4b52aca563": "/38.46da43978d7cabc2b1e8.chunk.js",
    "8d5c6914cdfb65340da458a21cc49d63fa3fc5e4": "/39.7e6d84c2104fc86bf3d1.chunk.js",
    "e0eb8d9bf610c88d1e5288f2243f5509ec5280b1": "/40.a0a8e0409b6734c6ba5d.chunk.js",
    "96af41341b2bc3300686eca6e86d5dd0050ea978": "/41.74ba03597aabcd784f20.chunk.js",
    "5e3de309375c2e0bcd2b43396d89a14394ea3299": "/42.f9d8214c338145751798.chunk.js",
    "3900811af3b6f33e387a5ebf76cb0aec84e7bdf7": "/43.2f7a184d48b626beac95.chunk.js",
    "048fa6789601a52868d59e628de84a813d49b9ea": "/44.b218f1d6933b32290f6f.chunk.js",
    "14b52e848f087be422ffe78ca5b24a27073a3f3c": "/45.c7beb01a0a0bbbd0e58d.chunk.js",
    "9ff28e678ddb42b28b16c775570cda5f4a05b329": "/46.66b25c6e7132659fc1d9.chunk.js",
    "c90febac4ba54fb9418e2a03c16c92f04a442a9b": "/47.b3e3a3b78fb30061f124.chunk.js",
    "755f49505ce9bd5d7ff5fb620ccbaa46caa698a2": "/48.45c8aa1b82af39f320ee.chunk.js",
    "2afd0fcb087d5d9d9038791fdd902dc6b6e36c61": "/49.55fd4cf74eeb4336ae01.chunk.js",
    "d5543ec09b9739b580324d025f9d6bace38a4ead": "/50.823f2507d3050ff4397f.chunk.js",
    "37787bd1185ef30fe5ae66916952a831a9e466b8": "/51.712da5da2c7c7a0b3506.chunk.js",
    "167c3b418e79e891815c6b95f52b725192b5c901": "/52.31cf34b750a2182d1831.chunk.js",
    "6b40272007033caaa289ceec9ea37ad151478a25": "/53.e5bcea0852f63497e20c.chunk.js",
    "f8d9b4dd156176c793f7c1a3ba733d579f8e7396": "/"
  },
  "strategy": "changed",
  "responseStrategy": "cache-first",
  "version": "9/25/2020, 9:28:55 AM",
  "name": "webpack-offline",
  "pluginVersion": "5.0.6",
  "relativePaths": false
};

/** *** */ (function(modules) { // webpackBootstrap
/** *** */ 	// The module cache
/** *** */ 	const installedModules = {};
  /** *** */
  /** *** */ 	// The require function
  /** *** */ 	function __webpack_require__(moduleId) {
    /** *** */
    /** *** */ 		// Check if module is in cache
    /** *** */ 		if(installedModules[moduleId]) {
      /** *** */ 			return installedModules[moduleId].exports;
      /** *** */ 		}
    /** *** */ 		// Create a new module (and put it into the cache)
    /** *** */ 		const module = installedModules[moduleId] = {
      /** *** */ 			i: moduleId,
      /** *** */ 			l: false,
      /** *** */ 			exports: {}
      /** *** */ 		};
    /** *** */
    /** *** */ 		// Execute the module function
    /** *** */ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
    /** *** */
    /** *** */ 		// Flag the module as loaded
    /** *** */ 		module.l = true;
    /** *** */
    /** *** */ 		// Return the exports of the module
    /** *** */ 		return module.exports;
    /** *** */ 	}
  /** *** */
  /** *** */
  /** *** */ 	// expose the modules object (__webpack_modules__)
  /** *** */ 	__webpack_require__.m = modules;
  /** *** */
  /** *** */ 	// expose the module cache
  /** *** */ 	__webpack_require__.c = installedModules;
  /** *** */
  /** *** */ 	// define getter function for harmony exports
  /** *** */ 	__webpack_require__.d = function(exports, name, getter) {
    /** *** */ 		if(!__webpack_require__.o(exports, name)) {
      /** *** */ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
      /** *** */ 		}
    /** *** */ 	};
  /** *** */
  /** *** */ 	// define __esModule on exports
  /** *** */ 	__webpack_require__.r = function(exports) {
    /** *** */ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
      /** *** */ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
      /** *** */ 		}
    /** *** */ 		Object.defineProperty(exports, '__esModule', { value: true });
    /** *** */ 	};
  /** *** */
  /** *** */ 	// create a fake namespace object
  /** *** */ 	// mode & 1: value is a module id, require it
  /** *** */ 	// mode & 2: merge all properties of value into the ns
  /** *** */ 	// mode & 4: return value when already ns object
  /** *** */ 	// mode & 8|1: behave like require
  /** *** */ 	__webpack_require__.t = function(value, mode) {
    /** *** */ 		if(mode & 1) value = __webpack_require__(value);
    /** *** */ 		if(mode & 8) return value;
    /** *** */ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
    /** *** */ 		const ns = Object.create(null);
    /** *** */ 		__webpack_require__.r(ns);
    /** *** */ 		Object.defineProperty(ns, 'default', { enumerable: true, value });
    /** *** */ 		if(mode & 2 && typeof value !== 'string') for(const key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
    /** *** */ 		return ns;
    /** *** */ 	};
  /** *** */
  /** *** */ 	// getDefaultExport function for compatibility with non-harmony modules
  /** *** */ 	__webpack_require__.n = function(module) {
    /** *** */ 		const getter = module && module.__esModule ?
    /** *** */ 			function getDefault() { return module.default; } :
    /** *** */ 			function getModuleExports() { return module; };
    /** *** */ 		__webpack_require__.d(getter, 'a', getter);
    /** *** */ 		return getter;
    /** *** */ 	};
  /** *** */
  /** *** */ 	// Object.prototype.hasOwnProperty.call
  /** *** */ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
  /** *** */
  /** *** */ 	// __webpack_public_path__
  /** *** */ 	__webpack_require__.p = "/";
  /** *** */
  /** *** */
  /** *** */ 	// Load entry module and return exports
  /** *** */ 	return __webpack_require__(__webpack_require__.s = "22249e1ea7baa06e7c1b");
/** *** */ })
/** ********************************************************************* */
/** *** */ ({

  /** */ "22249e1ea7baa06e7c1b":
  /** */ (function(module, exports, __webpack_require__) {

    


    (function () {
      const {waitUntil} = ExtendableEvent.prototype;
      const {respondWith} = FetchEvent.prototype;
      const promisesMap = new WeakMap();

      ExtendableEvent.prototype.waitUntil = function (promise) {
        const extendableEvent = this;
        let promises = promisesMap.get(extendableEvent);

        if (promises) {
          promises.push(Promise.resolve(promise));
          return;
        }

        promises = [Promise.resolve(promise)];
        promisesMap.set(extendableEvent, promises);

        // call original method
        return waitUntil.call(extendableEvent, Promise.resolve().then(function processPromises() {
          const len = promises.length;

          // wait for all to settle
          return Promise.all(promises.map(function (p) {
            return p.catch(function () {});
          })).then(function () {
            // have new items been added? If so, wait again
            if (promises.length != len) return processPromises();
            // we're done!
            promisesMap.delete(extendableEvent);
            // reject if one of the promises rejected
            return Promise.all(promises);
          });
        }));
      };

      FetchEvent.prototype.respondWith = function (promise) {
        this.waitUntil(promise);
        return respondWith.call(this, promise);
      };
    })();;
    'use strict';

    if (typeof DEBUG === 'undefined') {
      var DEBUG = false;
    }

    function WebpackServiceWorker(params, helpers) {
      const {cacheMaps} = helpers;
      // navigationPreload: true, { map: (URL) => URL, test: (URL) => boolean }
      const {navigationPreload} = helpers;

      // (update)strategy: changed, all
      const {strategy} = params;
      // responseStrategy: cache-first, network-first
      const {responseStrategy} = params;

      const {assets} = params;

      let {hashesMap} = params;
      let {externals} = params;

      const prefetchRequest = params.prefetchRequest || {
        credentials: 'same-origin',
        mode: 'cors'
      };

      const CACHE_PREFIX = params.name;
      const CACHE_TAG = params.version;
      const CACHE_NAME = `${CACHE_PREFIX  }:${  CACHE_TAG}`;

      const PRELOAD_CACHE_NAME = `${CACHE_PREFIX  }$preload`;
      const STORED_DATA_KEY = '__offline_webpack__data';

      mapAssets();

      const allAssets = [].concat(assets.main, assets.additional, assets.optional);

      self.addEventListener('install', function (event) {
        console.log('[SW]:', 'Install event');

        let installing;

        if (strategy === 'changed') {
          installing = cacheChanged('main');
        } else {
          installing = cacheAssets('main');
        }

        event.waitUntil(installing);
      });

      self.addEventListener('activate', function (event) {
        console.log('[SW]:', 'Activate event');

        let activation = cacheAdditional();

        // Delete all assets which name starts with CACHE_PREFIX and
        // is not current cache (CACHE_NAME)
        activation = activation.then(storeCacheData);
        activation = activation.then(deleteObsolete);
        activation = activation.then(function () {
          if (self.clients && self.clients.claim) {
            return self.clients.claim();
          }
        });

        if (navigationPreload && self.registration.navigationPreload) {
          activation = Promise.all([activation, self.registration.navigationPreload.enable()]);
        }

        event.waitUntil(activation);
      });

      function cacheAdditional() {
        if (!assets.additional.length) {
          return Promise.resolve();
        }

        if (DEBUG) {
          console.log('[SW]:', 'Caching additional');
        }

        let operation;

        if (strategy === 'changed') {
          operation = cacheChanged('additional');
        } else {
          operation = cacheAssets('additional');
        }

        // Ignore fail of `additional` cache section
        return operation.catch(function (e) {
          console.error('[SW]:', 'Cache section `additional` failed to load');
        });
      }

      function cacheAssets(section) {
        const batch = assets[section];

        return caches.open(CACHE_NAME).then(function (cache) {
          return addAllNormalized(cache, batch, {
            bust: params.version,
            request: prefetchRequest,
            failAll: section === 'main'
          });
        }).then(function () {
          logGroup(`Cached assets: ${  section}`, batch);
        }).catch(function (e) {
          console.error(e);
          throw e;
        });
      }

      function cacheChanged(section) {
        return getLastCache().then(function (args) {
          if (!args) {
            return cacheAssets(section);
          }

          const lastCache = args[0];
          const lastKeys = args[1];
          const lastData = args[2];

          const lastMap = lastData.hashmap;
          const lastVersion = lastData.version;

          if (!lastData.hashmap || lastVersion === params.version) {
            return cacheAssets(section);
          }

          const lastHashedAssets = Object.keys(lastMap).map(function (hash) {
            return lastMap[hash];
          });

          const lastUrls = lastKeys.map(function (req) {
            const url = new URL(req.url);
            url.search = '';
            url.hash = '';

            return url.toString();
          });

          const sectionAssets = assets[section];
          const moved = [];
          const changed = sectionAssets.filter(function (url) {
            if (lastUrls.indexOf(url) === -1 || lastHashedAssets.indexOf(url) === -1) {
              return true;
            }

            return false;
          });

          Object.keys(hashesMap).forEach(function (hash) {
            const asset = hashesMap[hash];

            // Return if not in sectionAssets or in changed or moved array
            if (sectionAssets.indexOf(asset) === -1 || changed.indexOf(asset) !== -1 || moved.indexOf(asset) !== -1) return;

            const lastAsset = lastMap[hash];

            if (lastAsset && lastUrls.indexOf(lastAsset) !== -1) {
              moved.push([lastAsset, asset]);
            } else {
              changed.push(asset);
            }
          });

          logGroup(`Changed assets: ${  section}`, changed);
          logGroup(`Moved assets: ${  section}`, moved);

          const movedResponses = Promise.all(moved.map(function (pair) {
            return lastCache.match(pair[0]).then(function (response) {
              return [pair[1], response];
            });
          }));

          return caches.open(CACHE_NAME).then(function (cache) {
            const move = movedResponses.then(function (responses) {
              return Promise.all(responses.map(function (pair) {
                return cache.put(pair[0], pair[1]);
              }));
            });

            return Promise.all([move, addAllNormalized(cache, changed, {
              bust: params.version,
              request: prefetchRequest,
              failAll: section === 'main',
              deleteFirst: section !== 'main'
            })]);
          });
        });
      }

      function deleteObsolete() {
        return caches.keys().then(function (keys) {
          const all = keys.map(function (key) {
            if (key.indexOf(CACHE_PREFIX) !== 0 || key.indexOf(CACHE_NAME) === 0) return;

            console.log('[SW]:', 'Delete cache:', key);
            return caches.delete(key);
          });

          return Promise.all(all);
        });
      }

      function getLastCache() {
        return caches.keys().then(function (keys) {
          let index = keys.length;
          let key;

          while (index--) {
            key = keys[index];

            if (key.indexOf(CACHE_PREFIX) === 0) {
              break;
            }
          }

          if (!key) return;

          let cache;

          return caches.open(key).then(function (_cache) {
            cache = _cache;
            return _cache.match(new URL(STORED_DATA_KEY, location).toString());
          }).then(function (response) {
            if (!response) return;

            return Promise.all([cache, cache.keys(), response.json()]);
          });
        });
      }

      function storeCacheData() {
        return caches.open(CACHE_NAME).then(function (cache) {
          const data = new Response(JSON.stringify({
            version: params.version,
            hashmap: hashesMap
          }));

          return cache.put(new URL(STORED_DATA_KEY, location).toString(), data);
        });
      }

      self.addEventListener('fetch', function (event) {
        // Handle only GET requests
        if (event.request.method !== 'GET') {
          return;
        }

        // This prevents some weird issue with Chrome DevTools and 'only-if-cached'
        // Fixes issue #385, also ref to:
        // - https://github.com/paulirish/caltrainschedule.io/issues/49
        // - https://bugs.chromium.org/p/chromium/issues/detail?id=823392
        if (event.request.cache === 'only-if-cached' && event.request.mode !== 'same-origin') {
          return;
        }

        const url = new URL(event.request.url);
        url.hash = '';

        let urlString = url.toString();

        // Not external, so search part of the URL should be stripped,
        // if it's external URL, the search part should be kept
        if (externals.indexOf(urlString) === -1) {
          url.search = '';
          urlString = url.toString();
        }

        let assetMatches = allAssets.indexOf(urlString) !== -1;
        let cacheUrl = urlString;

        if (!assetMatches) {
          const cacheRewrite = matchCacheMap(event.request);

          if (cacheRewrite) {
            cacheUrl = cacheRewrite;
            assetMatches = true;
          }
        }

        if (!assetMatches) {
          // Use request.mode === 'navigate' instead of isNavigateRequest
          // because everything what supports navigationPreload supports
          // 'navigate' request.mode
          if (event.request.mode === 'navigate') {
            // Requesting with fetchWithPreload().
            // Preload is used only if navigationPreload is enabled and
            // navigationPreload mapping is not used.
            if (navigationPreload === true) {
              event.respondWith(fetchWithPreload(event));
              return;
            }
          }

          // Something else, positive, but not `true`
          if (navigationPreload) {
            const preloadedResponse = retrivePreloadedResponse(event);

            if (preloadedResponse) {
              event.respondWith(preloadedResponse);
              return;
            }
          }

          // Logic exists here if no cache match
          return;
        }

        // Cache handling/storing/fetching starts here
        let resource;

        if (responseStrategy === 'network-first') {
          resource = networkFirstResponse(event, urlString, cacheUrl);
        }
        // 'cache-first' otherwise
        // (responseStrategy has been validated before)
        else {
          resource = cacheFirstResponse(event, urlString, cacheUrl);
        }

        event.respondWith(resource);
      });

      self.addEventListener('message', function (e) {
        const {data} = e;
        if (!data) return;

        switch (data.action) {
          case 'skipWaiting':
            {
              if (self.skipWaiting) self.skipWaiting();
            }break;
        }
      });

      function cacheFirstResponse(event, urlString, cacheUrl) {
        handleNavigationPreload(event);

        return cachesMatch(cacheUrl, CACHE_NAME).then(function (response) {
          if (response) {
            if (DEBUG) {
              console.log('[SW]:', `URL [${  cacheUrl  }](${  urlString  }) from cache`);
            }

            return response;
          }

          // Load and cache known assets
          const fetching = fetch(event.request).then(function (response) {
            if (!response.ok) {
              if (DEBUG) {
                console.log('[SW]:', `URL [${  urlString  }] wrong response: [${  response.status  }] ${  response.type}`);
              }

              return response;
            }

            if (DEBUG) {
              console.log('[SW]:', `URL [${  urlString  }] from network`);
            }

            if (cacheUrl === urlString) {
              (function () {
                const responseClone = response.clone();
                const storing = caches.open(CACHE_NAME).then(function (cache) {
                  return cache.put(urlString, responseClone);
                }).then(function () {
                  console.log('[SW]:', `Cache asset: ${  urlString}`);
                });

                event.waitUntil(storing);
              })();
            }

            return response;
          });

          return fetching;
        });
      }

      function networkFirstResponse(event, urlString, cacheUrl) {
        return fetchWithPreload(event).then(function (response) {
          if (response.ok) {
            if (DEBUG) {
              console.log('[SW]:', `URL [${  urlString  }] from network`);
            }

            return response;
          }

          // Throw to reach the code in the catch below
          throw response;
        })
        // This needs to be in a catch() and not just in the then() above
        // cause if your network is down, the fetch() will throw
          .catch(function (erroredResponse) {
            if (DEBUG) {
              console.log('[SW]:', `URL [${  urlString  }] from cache if possible`);
            }

            return cachesMatch(cacheUrl, CACHE_NAME).then(function (response) {
              if (response) {
                return response;
              }

              if (erroredResponse instanceof Response) {
                return erroredResponse;
              }

              // Not a response at this point, some other error
              throw erroredResponse;
              // return Response.error();
            });
          });
      }

      function handleNavigationPreload(event) {
        if (navigationPreload && typeof navigationPreload.map === 'function' &&
    // Use request.mode === 'navigate' instead of isNavigateRequest
    // because everything what supports navigationPreload supports
    // 'navigate' request.mode
    event.preloadResponse && event.request.mode === 'navigate') {
          const mapped = navigationPreload.map(new URL(event.request.url), event.request);

          if (mapped) {
            storePreloadedResponse(mapped, event);
          }
        }
      }

      // Temporary in-memory store for faster access
      const navigationPreloadStore = new Map();

      function storePreloadedResponse(_url, event) {
        const url = new URL(_url, location);
        const preloadResponsePromise = event.preloadResponse;

        navigationPreloadStore.set(preloadResponsePromise, {
          url,
          response: preloadResponsePromise
        });

        const isSamePreload = function isSamePreload() {
          return navigationPreloadStore.has(preloadResponsePromise);
        };

        const storing = preloadResponsePromise.then(function (res) {
          // Return if preload isn't enabled or hasn't happened
          if (!res) return;

          // If navigationPreloadStore already consumed
          // or navigationPreloadStore already contains another preload,
          // then do not store anything and return
          if (!isSamePreload()) {
            return;
          }

          const clone = res.clone();

          // Storing the preload response for later consume (hasn't yet been consumed)
          return caches.open(PRELOAD_CACHE_NAME).then(function (cache) {
            if (!isSamePreload()) return;

            return cache.put(url, clone).then(function () {
              if (!isSamePreload()) {
                return caches.open(PRELOAD_CACHE_NAME).then(function (cache) {
                  return cache.delete(url);
                });
              }
            });
          });
        });

        event.waitUntil(storing);
      }

      function retriveInMemoryPreloadedResponse(url) {
        if (!navigationPreloadStore) {
          return;
        }

        let foundResponse;
        let foundKey;

        navigationPreloadStore.forEach(function (store, key) {
          if (store.url.href === url.href) {
            foundResponse = store.response;
            foundKey = key;
          }
        });

        if (foundResponse) {
          navigationPreloadStore.delete(foundKey);
          return foundResponse;
        }
      }

      function retrivePreloadedResponse(event) {
        const url = new URL(event.request.url);

        if (self.registration.navigationPreload && navigationPreload && navigationPreload.test && navigationPreload.test(url, event.request)) {} else {
          return;
        }

        const fromMemory = retriveInMemoryPreloadedResponse(url);
        const {request} = event;

        if (fromMemory) {
          event.waitUntil(caches.open(PRELOAD_CACHE_NAME).then(function (cache) {
            return cache.delete(request);
          }));

          return fromMemory;
        }

        return cachesMatch(request, PRELOAD_CACHE_NAME).then(function (response) {
          if (response) {
            event.waitUntil(caches.open(PRELOAD_CACHE_NAME).then(function (cache) {
              return cache.delete(request);
            }));
          }

          return response || fetch(event.request);
        });
      }

      function mapAssets() {
        Object.keys(assets).forEach(function (key) {
          assets[key] = assets[key].map(function (path) {
            const url = new URL(path, location);

            url.hash = '';

            if (externals.indexOf(path) === -1) {
              url.search = '';
            }

            return url.toString();
          });
        });

        hashesMap = Object.keys(hashesMap).reduce(function (result, hash) {
          const url = new URL(hashesMap[hash], location);
          url.search = '';
          url.hash = '';

          result[hash] = url.toString();
          return result;
        }, {});

        externals = externals.map(function (path) {
          const url = new URL(path, location);
          url.hash = '';

          return url.toString();
        });
      }

      function addAllNormalized(cache, requests, options) {
        const bustValue = options.bust;
        const failAll = options.failAll !== false;
        const deleteFirst = options.deleteFirst === true;
        const requestInit = options.request || {
          credentials: 'omit',
          mode: 'cors'
        };

        let deleting = Promise.resolve();

        if (deleteFirst) {
          deleting = Promise.all(requests.map(function (request) {
            return cache.delete(request).catch(function () {});
          }));
        }

        return Promise.all(requests.map(function (request) {
          if (bustValue) {
            request = applyCacheBust(request, bustValue);
          }

          return fetch(request, requestInit).then(fixRedirectedResponse).then(function (response) {
            if (!response.ok) {
              return { error: true };
            }

            return { response };
          }, function () {
            return { error: true };
          });
        })).then(function (responses) {
          if (failAll && responses.some(function (data) {
            return data.error;
          })) {
            return Promise.reject(new Error('Wrong response status'));
          }

          if (!failAll) {
            responses = responses.filter(function (data) {
              return !data.error;
            });
          }

          return deleting.then(function () {
            const addAll = responses.map(function (_ref, i) {
              const {response} = _ref;

              return cache.put(requests[i], response);
            });

            return Promise.all(addAll);
          });
        });
      }

      function matchCacheMap(request) {
        const urlString = request.url;
        const url = new URL(urlString);

        let requestType;

        if (isNavigateRequest(request)) {
          requestType = 'navigate';
        } else if (url.origin === location.origin) {
          requestType = 'same-origin';
        } else {
          requestType = 'cross-origin';
        }

        for (let i = 0; i < cacheMaps.length; i++) {
          const map = cacheMaps[i];

          if (!map) continue;
          if (map.requestTypes && map.requestTypes.indexOf(requestType) === -1) {
            continue;
          }

          let newString;

          if (typeof map.match === 'function') {
            newString = map.match(url, request);
          } else {
            newString = urlString.replace(map.match, map.to);
          }

          if (newString && newString !== urlString) {
            return newString;
          }
        }
      }

      function fetchWithPreload(event) {
        if (!event.preloadResponse || navigationPreload !== true) {
          return fetch(event.request);
        }

        return event.preloadResponse.then(function (response) {
          return response || fetch(event.request);
        });
      }
    }

    function cachesMatch(request, cacheName) {
      return caches.match(request, {
        cacheName
      }).then(function (response) {
        if (isNotRedirectedResponse(response)) {
          return response;
        }

        // Fix already cached redirected responses
        return fixRedirectedResponse(response).then(function (fixedResponse) {
          return caches.open(cacheName).then(function (cache) {
            return cache.put(request, fixedResponse);
          }).then(function () {
            return fixedResponse;
          });
        });
      })
      // Return void if error happened (cache not found)
        .catch(function () {});
    }

    function applyCacheBust(asset, key) {
      const hasQuery = asset.indexOf('?') !== -1;
      return `${asset + (hasQuery ? '&' : '?')  }__uncache=${  encodeURIComponent(key)}`;
    }

    function isNavigateRequest(request) {
      return request.mode === 'navigate' || request.headers.get('Upgrade-Insecure-Requests') || (request.headers.get('Accept') || '').indexOf('text/html') !== -1;
    }

    function isNotRedirectedResponse(response) {
      return !response || !response.redirected || !response.ok || response.type === 'opaqueredirect';
    }

    // Based on https://github.com/GoogleChrome/sw-precache/pull/241/files#diff-3ee9060dc7a312c6a822cac63a8c630bR85
    function fixRedirectedResponse(response) {
      if (isNotRedirectedResponse(response)) {
        return Promise.resolve(response);
      }

      const body = 'body' in response ? Promise.resolve(response.body) : response.blob();

      return body.then(function (data) {
        return new Response(data, {
          headers: response.headers,
          status: response.status
        });
      });
    }

    function copyObject(original) {
      return Object.keys(original).reduce(function (result, key) {
        result[key] = original[key];
        return result;
      }, {});
    }

    function logGroup(title, assets) {
      console.groupCollapsed('[SW]:', title);

      assets.forEach(function (asset) {
        console.log('Asset:', asset);
      });

      console.groupEnd();
    }
    WebpackServiceWorker(__wpo, {
      loaders: {},
      cacheMaps: [
        {
          match(url) {
            if (url.pathname === location.pathname) {
              return;
            }

            return new URL("/", location);
          },
          to: null,
          requestTypes: ["navigate"],
        }
      ],
      navigationPreload: false,
    });
    module.exports = __webpack_require__("6872a71ed75a597694c7")
      

    /** */ }),

  /** */ "6872a71ed75a597694c7":
  /** */ (function(module, exports) {



    /** */ })

/** *** */ });